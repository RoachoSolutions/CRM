@if (ViewBag.Title != null)
{
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>@ViewBag.Title</h2>
        @*<ol class="breadcrumb">
                <li class="active">
                    <strong><a href="@Url.Action("Index", "Inicio", new { area="Prospectos" })">Inicio</a></strong>
                </li>
            </ol>*@
    </div>
    <div class="col-lg-2">

    </div>
</div>
}