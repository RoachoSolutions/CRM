@if (ViewBag.Title != null)
{
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2><a href="@Url.CustomUrlArea("")">@ViewBag.Title</a></h2>
    </div>
    <div class="col-lg-2">

    </div>
</div>
}